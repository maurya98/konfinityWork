const gk=(obj)=>{
  var a=0,b=[];
  for(i=0;i<obj.length;i++){
    if(obj[i].id===null||isNaN(obj[i].id)||obj[i].id===0||obj[i].id==='undefined')
      a++;
    else
      b.push(obj[i]);
  }
  var o={prop1:b,prop2:a};
  return o;
};
module.exports=gk;
