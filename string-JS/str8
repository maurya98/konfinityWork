const gk=(str)=>{
  var a='';
  var arr=str.split('');
  for(var i=0;i<arr.length;i++){
    if(arr[i].toUpperCase()===arr[i])
      arr[i]=arr[i].toLowerCase();
    else
      arr[i]=arr[i].toUpperCase();
    a=a+arr[i];
  }
  return a;
};
module.exports=gk;
