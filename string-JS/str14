const gk=(str,c,f)=>{
  var s=str.indexOf(c);
  if(s>=0){
    if(f===1)
      return str.slice(0,s);
    else if(f===2)
      return str.slice(s+1);
  }
  else
    return false;
};
module.exports=gk;
