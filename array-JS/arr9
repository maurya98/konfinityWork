const gk=(a)=>{
  var d=[];
  for(var i=0;i<a.length;i++){
    for(var j=i+1;j<a.length;j++){
      if(a[i]===a[j]){
        d.push(a[i]);
        break;
      }
    }
  }
  return d;
};
module.exports=gk;
