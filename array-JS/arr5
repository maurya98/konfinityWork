const gk=(a)=>{
  for (var i=1; i<a.length;i++){
    for (var j=0;j<i;j++){
      if (a[i]<a[j]) {
        var[element]=a.splice(i,1);
        a.splice(j,0,element);
      }
    }
  }
  return a;
};
module.exports=gk;
