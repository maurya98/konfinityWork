const gk=(a,n)=>{
  var arr=[];
  for(var i=0;i<a.length;i++){
    for(var j=i+1;j<a.length;j++){
      if(a[i]+a[j]===n){
        arr.push(i,j);
      }
    }
  }
  if(arr.length===0)
    return false;
  else
    return arr;
};
module.exports=gk;
