const prime=(n)=>{
  var total=0;
  for(var i=2;i<n;i++){
    var c=false;
    for(var j=2;j<=i;j++){
      if(i%j===0&&i!==j){
        c=true;
      }
    }
    if(c===false){
      total++;
    }
  }
  return(total);
};
module.exports=prime;
