const times=(n)=>{
var sum=100;
var count=0;
if(n<10){
    return(0);
  }
  while(n>9){
    sum=0;
    count++;
    while(n>0){
      sum=sum+(n%10);
      n=parseInt(n/10);
    }
    n=sum;
  }
  return(count);
};
module.exports=times;
