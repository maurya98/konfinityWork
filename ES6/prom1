var x='Hello';
let hello=(x)=>{
  return new Promise((resolve, reject) => {
    var err=x;
    if((err=x))
      resolve("Hello world!");
    else
      reject("Bye!");
  });
};

hello(x).then(result=>{
  return result;
}).catch(er=>{
  return er;
});

module.exports = {
  s: x,
  hello: hello
};
