const bina=(x)=>{
  return new Promise((resolve, reject) => {
    var a=x;
    if((a=0))
      resolve("YES");
    else
      reject("NO");
  });
};

bina(1).then(result=>{
  return result;
}).catch(er=>{
  return er;
});

module.exports = {
  bina: bina
};
